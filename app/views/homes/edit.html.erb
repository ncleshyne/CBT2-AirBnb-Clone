<div class="coverImage" style="background-image: url('<%= @home.image.url %>');" aria-hidden="true"></div>
<%= render 'form' %>
<div class="text-center edit-links">
  <%= link_to "Delete home", home_path(@home), method: :delete, data: { confirm: "Are you sure you want to delete this home?" } %>
  <%= link_to "cancel", homes_path %>
</div>
